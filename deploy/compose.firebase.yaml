services:
  backend:
    environment:
      FIREBASE_PUSH_ENABLED: "true"
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/firebase-service-account.json
    volumes:
      - ./deploy/secrets/firebase-service-account.json:/run/secrets/firebase-service-account.json:ro
